# =============================================================================
# Marmot + PortalJS Full Stack - Environment Configuration
# =============================================================================
# Copy this file to .env and configure as needed.
#
# IMPORTANT: After first startup, create an API key in Marmot UI or via script:
#   ./scripts/00_init-api-key.sh
# =============================================================================

# =============================================================================
# REQUIRED: Marmot Connection
# =============================================================================

# Marmot Instance URL
# - For local development (without Docker): http://localhost:8080
# - For Docker deployment: set automatically by docker-compose.yml
NEXT_PUBLIC_DMS=http://localhost:8080

# Marmot API Key
# Created in the Marmot UI under Settings > API Keys, or via the init script.
# Leave empty for initial deployment, then update after creating an API key.
DMS_API_KEY=

# =============================================================================
# OPTIONAL: Display Configuration
# =============================================================================

# Portal title (shown in browser tab and header)
NEXT_PUBLIC_PORTAL_TITLE=Data Catalog

# Theme accent color (hex)
NEXT_PUBLIC_THEME_COLOR=#4977AB

# =============================================================================
# OPTIONAL: Marmot Configuration (Docker only)
# =============================================================================
# These have sensible defaults, only change if needed.

# PostgreSQL password for Marmot database
POSTGRES_PASSWORD=marmot

# Marmot encryption key (generate with: marmot generate-encryption-key)
MARMOT_ENCRYPTION_KEY=

# Logging level (debug, info, warn, error)
MARMOT_LOGGING_LEVEL=info
